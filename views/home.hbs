{{> navbar }}



<section class="hero-section">
  <div class="img"><img src="/images/friends.svg" alt=""></div>
  <div class="title">
    <h1>Välkommen!</h1>
    <h2>Detta är en sida om mina vänner och våra resor tillsammans.</h2>
  </div>

</section>

<section class="friends-section">
  <div class="container">

    {{#each friends }}
    <div class="friend-card">

      <div class="image">
        <img src="{{this.url}}">
      </div>

      <div class="presentation">
        <h1> {{this.name}}</h1>
        <p class="label">Om:</p>
        <p> {{this.about}}</p>
        <p class="label">Intressen:</p>
        <ul>
          {{#each this.interests}}
          <li>{{this}}</li>
          {{/each}}
        </ul>
      </div>
    </div>
    {{/each}}
  </div>


</section>