window.addEventListener("load", () => {
	// document.querySelector(`table.tab`).classList.add("active");

	// let tabLinks = document.querySelectorAll(`li[data-type="tab"]`);
	// tabLinks.forEach((element) => {
	// 	element.addEventListener("click", () => switchTab(element));
	// });

	
});

// function switchTab(element) {
// 	let tab = element.getAttribute("data-id");
// 	document
// 		.querySelectorAll("table.tab")
// 		.forEach((e) => e.classList.remove("active"));
// 	document.getElementById(tab).classList.add("active");
// }