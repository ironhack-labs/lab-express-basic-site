<div class="gallery-page">
    <div class="section-header">
        <h2 class="section-header__title">{{ title }}</h2>
    </div>

    <div class="container">
        <div class="gallery">
            {{#each photos}}
                {{> photo photo=this}}
            {{/each}}
        </div>
    </div>
</div>