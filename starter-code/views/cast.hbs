<h2 class="text-center mt-5 mb-5">{{pageTitle}}</h2>
<div class="container">
    <div class="row justify-content-center">
        {{#each people}}
        {{#with this}}
            {{> card}}
        {{/with}}
        {{/each}}
    </div>
    <div class="row justify-content-center mt-2">
       <a href="/gallery" class="btn btn-dark">Back to Gallery</a>
    </div>
</div>
