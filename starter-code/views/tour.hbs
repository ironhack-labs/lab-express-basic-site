<h1 class="title neon">Tour</h1>
<div class="container container__tour">
    <div class="dates">
        <ul>
            {{#each dates}}
            {{#with this}}
            {{> event}}
            {{/with}}
            {{/each}}
        </ul>
    </div>
</div>